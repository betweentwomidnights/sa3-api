# Vendored inference source — regenerated by ./sync_engine.sh (commit pinned
# in engine/SOURCE_COMMIT). Not committed to keep this repo small.
engine/

# Blessed LoRA checkpoints — copied in per release, large binaries.
loras/*.ckpt
loras/*.safetensors

outputs/
*.wav
*.mp3
__pycache__/
*.pyc
.venv/
